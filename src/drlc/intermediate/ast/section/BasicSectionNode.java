package drlc.intermediate.ast.section;

import drlc.node.Node;

public abstract class BasicSectionNode extends ProgramSectionNode {
	
	protected BasicSectionNode(Node[] parseNodes) {
		super(parseNodes);
	}
}
