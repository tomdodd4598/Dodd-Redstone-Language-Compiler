Add 'typeof expression' expression

Add 'nameof expression' expression

Add 'macro! name(...) { ... }' syntax

Generics/templates

Generic slices/ranges

For loops

Pattern matching

Enums (nominal typing)

Implement program arguments

Small standard library
